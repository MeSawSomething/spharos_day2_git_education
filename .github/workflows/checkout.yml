name: Our Workflow
on: push
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - run: ls -al
      - uses: actions/checkout@v3
      - run : ls al #작업디렉토리의 파일 나열
      - run: cat .github/workflows/checkout.yml #워크플로우 YAML 내용 출력
